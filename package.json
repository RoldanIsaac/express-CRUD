{
  "name": "express-starter",
  "version": "1.0.0",
  "description": "Express Starter Server",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node server",
    "dev": "node --watch --env-file=.env server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Orlando Llamos Isaac",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2"
  }
}
